rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read for all paths (listing + download); write requires auth
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
